@model LoginViewModel
@{
    ViewData["Title"] = "Company Login";
}
<div class="auth-page company-auth-bg">
    <div class="auth-breadcrumb">Company Login</div>
    <div class="auth-outer auth-outer-right">
        <a href="/" class="back-link">&#8592; Back to Home</a>
        <div class="auth-card">
            <h2>Company Login</h2>
            <form asp-action="Login" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger small"></div>
                <div class="form-group">
                    <label>Email</label>
                    <input asp-for="Email" class="form-input" placeholder="Enter your Email ( XXX@gmail.my )" />
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input asp-for="Password" type="password" class="form-input" placeholder="Enter your Password" />
                </div>
                <div class="form-row-between">
                    <label class="checkbox-label"><input asp-for="RememberMe" type="checkbox" /> Remember me</label>
                    <a href="#" class="link-muted">Forgot Password ?</a>
                </div>
                <button type="submit" class="btn-dark btn-full mt-2">Login</button>
            </form>
            <div class="auth-footer">Doesn't have an Account ? <a href="/CompanyAuth/Register">Register Here</a></div>
        </div>
    </div>
</div>
